export interface IPensamento {
  id?: number;
  conteudo: string;
  autoria: string;
  modelo: string;
}

// A interface é um "contrato" de que todos os dados devem seguir os tipos definidos
